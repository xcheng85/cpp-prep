#include <vector>
#include <string>
#include <numeric>
#include <queue>
#include <unordered_set>
#include <iterator>

using namespace std;
// very similar to 57, insert interval

class Solution {
public:
    vector<vector<int>> merge(vector<vector<int>>& intervals) {
        // 1 <= intervals.length <= 104
        // start current interval without worrying edge case

        vector<int> current = intervals[0];
        
    }
};